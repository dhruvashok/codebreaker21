from email import message_from_file

# for i in range(24):
	# f = message_from_file(open(f"message_{i}.eml"))
	# for x in len(f.get_payload()):
		# if f.get_payload()[x].get_content_type() != "text/plain":
		# open(f"file_{i}_{x}", "wb").write(f.get_payload()[x].get_payload(decode=True))
